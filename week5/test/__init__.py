# this file tells python that it should treat this folder as a package <- python module(s) organized inside a folder
# using pytest test (THE WHOLE FOLDER), pytest will test the whole folder if it has multiple files with multiple tests

